import React from "react";

import Layout from "../../components/layout";
import Title from "../../components/title";

export default () => (
  <Layout title="writing">
    <section className="center-children">
      <header>
        <Title text="writing" />
      </header>
      <p>...just kidding, not yet :)</p>
    </section>
  </Layout>
);
